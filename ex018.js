/*
Escreva uma função que converta a string de entrada para letras maiúsculas.*/

function makeUpperCase(str) {
    return str.toUpperCase()
}

console.log(makeUpperCase("hello!"))
console.log(makeUpperCase("Good morning!"))